<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nishchal.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher007"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dialog_specifications"
            android:theme="@style/AppTheme1"></activity>

<activity
    android:name=".Completed_tasks"
    android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".Incompleted_tasks"
            android:theme="@style/AppTheme"></activity>

    <activity
        android:name=".About"
        android:theme="@style/AppTheme"></activity>
        <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>﻿

        <receiver android:name=".NotificationReciever"/>

    </application>
</manifest>